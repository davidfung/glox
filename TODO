NEXT
- fix: repl() why &(input.Text()) does not work?
- type define InterpretResult
- advance() in both compiler and scanner
- number() in both compiler and scanner
  - move into different package?

TODO
