fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var n = 35;

var start = clock();
print fib(n);
print clock() - start;

start = clock();
print fibnative(n);
print clock() - start;

